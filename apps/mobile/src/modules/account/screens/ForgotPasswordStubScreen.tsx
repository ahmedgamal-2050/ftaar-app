import React from 'react';
import { ScreenPlaceholder } from '../../../ui';

export function ForgotPasswordStubScreen() {
  return (
    <ScreenPlaceholder
      name="ForgotPasswordStub"
      description="Placeholder for the password reset flow — the route exists so Login has somewhere to send people."
    />
  );
}
