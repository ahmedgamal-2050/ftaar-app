/**
 * Stand-in lobby code for the placeholder navigation links. Real codes arrive
 * with the lobby create/join feature; until then this keeps every route in the
 * LobbyStack reachable by tapping.
 */
export const SAMPLE_LOBBY_CODE = 'FTAAR1';
